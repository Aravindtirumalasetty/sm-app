specVersion: "v1beta"
serviceId: "sm-app"
location: "asia-east1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "sm-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
